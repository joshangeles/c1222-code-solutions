/* exported toObject */
/* make empty object to store key-value pair
   use the first value found in the array as key
   use the second value found in the array as value
   use bracket or dot notation to add key value pair to empty object */
function toObject(keyValuePair) {
  var object = {};
  object.keyValuePair[0] = 1;
}
